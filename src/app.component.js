import { Component, Inject } from '@angular/core';

@Component({
  selector: 'my-app',
  template: `
    <h1>Random Quotes</h1>
    <random-quote></random-quote>
  `      
})

export class AppComponent { } 
